# dependencies
node_modules/

# build output
dist/

# per-project build workspaces + saved state (generated, can be large)
.workspace/

# local, user-specific generated config (never source)
registry.overrides.json
findings.json

# logs / os cruft
*.log
.DS_Store
